#include <stdio.h>
int main() 
{
    int x; /* declare an integer variable called x */
    printf( "Declare x first\n" ); /* variables must be declared before other code within the block */

    return 0; /* return error code 0 (no error) */
}
