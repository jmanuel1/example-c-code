#include <stdio.h> /* include the entire contents of stdio.h at the beginning of this program */

int main() /* main() is of type int (it returns an integer)
	      every program must start in main() */
{
	printf("Hello, world!\n"); /* the printf function prints strings
				      literal strings are enclosed in quotes, and the sequence "\n" specifies the newline character */
}
